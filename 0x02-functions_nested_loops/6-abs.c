#include "main.h"

/**
 * _abs - computes the absolute integer
 *
 * @i: the number to check
 *
 * Return: The absolute value of i
 */

int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	return (-i);
}
