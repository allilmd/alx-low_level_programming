#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: Numbers
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf("%d", i);
	}

	putchar(10);
	return (0);
}
